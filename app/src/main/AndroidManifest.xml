<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sweng888.edu.psu.androiduiandlogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginSuccessActivity"
            android:label="success_login" />

        <activity android:name=".SignUpActivity" android:label="User Sign Up">

        </activity>

        <activity android:name=".ViewAllUsersActivity" android:label="User Sign Up">

        </activity>

    </application>

</manifest>